import Vue from 'vue';
import VueRouter from 'vue-router';


Vue.use(VueRouter)

const routes = [
  // {
  //   path:"/",
  //   components:
  // }
]

const router = new VueRouter({
  routes:routes
})

export default router
